[www]
; Pass container environment variables (APP_ENV, APP_SECRET, ...) through to
; PHP. The PHP-FPM default is clear_env = yes, which would otherwise strip
; them and silently leave the app running in dev mode.
clear_env = no
